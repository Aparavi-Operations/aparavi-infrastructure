---

- ansible.builtin.import_tasks: 'install-docker.yml'
  become: true
  tags:
    - 'docker'

- ansible.builtin.import_tasks: 'install-docker-compose.yml'
  tags:
    - 'compose'
  when: 'docker_compose_install | bool'
